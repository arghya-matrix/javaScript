// Global Scope: available during the whole program
// function Block: Available during the function block only
// if block: can be accessed in if block only
// Scope of Var: Var gives separate scope to function block only.